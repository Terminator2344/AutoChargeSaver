# Анализ переменных окружения проекта

## 📍 Основной файл инициализации и валидации

**Полный путь:** `app/src/config/env.ts`

Это единственный файл, где происходит валидация и инициализация всех переменных окружения.

---

## 📋 Расположение переменных по файлам

### 1. `app/src/config/env.ts` — Основной файл валидации

#### Строки 6-51: Определение схемы валидации (EnvSchema)
```typescript
const EnvSchema = z.object({
  PORT: z.string().default('3000'),                    // строка 7
  APP_HOST: z.string().url(),                         // строка 8
  
  WHOP_API_KEY: z.string().optional(),                // строка 11
  WHOP_CLIENT_ID: z.string().optional(),              // строка 12
  WHOP_CLIENT_SECRET: z.string().optional(),         // строка 13
  WHOP_REDIRECT_URI: z.string().url().optional(),    // строка 14
  NEXT_PUBLIC_WHOP_APP_ID: z.string().optional(),    // строка 15
  NEXT_PUBLIC_WHOP_AGENT_USER_ID: z.string().optional(), // строка 16
  NEXT_PUBLIC_WHOP_COMPANY_ID: z.string().optional(), // строка 17
  SESSION_SECRET: z.string().optional(),             // строка 18
});
```

#### Строка 53: Парсинг из process.env
```typescript
const parsed = EnvSchema.safeParse(process.env);  // строка 53
```

#### Строки 65-70: Обработка и проверки
```typescript
const envRaw = parsed.data;                        // строка 65
if (!envRaw.WHOP_API_KEY) {                        // строка 68
  console.warn('⚠️ Missing WHOP_API_KEY...');     // строка 69
}
```

#### Строки 79-100: Экспорт валидированных значений
```typescript
export const env = {
  ...envRaw,                                        // строка 80
  PORT: Number(envRaw.PORT || '3000'),             // строка 81
  WHOP_API_KEY: envRaw.WHOP_API_KEY,              // строка 83
  NEXT_PUBLIC_WHOP_APP_ID: envRaw.NEXT_PUBLIC_WHOP_APP_ID, // строка 84
  NEXT_PUBLIC_WHOP_AGENT_USER_ID: envRaw.NEXT_PUBLIC_WHOP_AGENT_USER_ID, // строка 85
  NEXT_PUBLIC_WHOP_COMPANY_ID: envRaw.NEXT_PUBLIC_WHOP_COMPANY_ID, // строка 86
  // ... остальные переменные
};
```

**Важно:** Все переменные из списка берутся из `process.env` через Zod валидацию. Никаких хардкод значений для этих переменных нет.

---

### 2. Места использования переменных (только чтение)

#### `app/src/server.ts`
- **Строка 88:** `env.APP_HOST` — используется для CORS origin
- **Строка 129:** `env.SESSION_SECRET || 'dev-secret'` — fallback только для локальной разработки (если SESSION_SECRET не задан)

#### `app/src/auth/whopOAuth.ts`
- **Строка 20:** `process.env.WHOP_CLIENT_ID` — только для логирования (не используется в логике)
- **Строка 24:** `env.WHOP_CLIENT_ID`, `env.WHOP_CLIENT_SECRET`, `env.WHOP_REDIRECT_URI` — проверка наличия
- **Строка 104:** `env.WHOP_CLIENT_ID` — OAuth URL
- **Строка 105:** `env.WHOP_REDIRECT_URI` — OAuth URL
- **Строки 136-137:** `env.WHOP_CLIENT_ID`, `env.WHOP_CLIENT_SECRET` — OAuth token exchange
- **Строка 140:** `env.WHOP_REDIRECT_URI` — OAuth token exchange

#### `app/src/integrations/whop.ts`
- **Строка 38:** `env.WHOP_API_KEY` — создание Whop API клиента

#### `app/src/tracking/clickRedirect.ts`
- **Строка 11:** `env.APP_HOST` — генерация redirect URL

---

## 🔍 Источники переменных

### ✅ Все переменные берутся из `process.env`

**Процесс загрузки:**
1. **Строка 4:** `loadEnv()` — загружает переменные из `.env` файла через `dotenv`
2. **Строка 53:** `EnvSchema.safeParse(process.env)` — валидация всех переменных из `process.env`
3. **Строки 79-100:** Экспорт валидированных значений в объект `env`

**Источники по приоритету:**
1. Переменные окружения системы (устанавливаются ОС или Vercel/Render/etc.)
2. Файл `.env` в корне проекта (загружается через `dotenv` на строке 4)
3. Файл `.env.local` (загружается через `dotenv` на строке 4, если существует)

### ❌ Хардкод значений: НЕТ

**Единственный fallback:**
- `env.SESSION_SECRET || 'dev-secret'` в `app/src/server.ts:129`
  - Это fallback значение, используется ТОЛЬКО если `SESSION_SECRET` не задан
  - Используется только для локальной разработки
  - В production должно быть установлено `SESSION_SECRET`

---

## 📊 Таблица переменных

| Переменная | Источник | Валидация | Обязательная | Хардкод |
|------------|----------|-----------|--------------|---------|
| `WHOP_API_KEY` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `NEXT_PUBLIC_WHOP_APP_ID` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `NEXT_PUBLIC_WHOP_AGENT_USER_ID` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `NEXT_PUBLIC_WHOP_COMPANY_ID` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `WHOP_CLIENT_ID` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `WHOP_CLIENT_SECRET` | `process.env` | `z.string().optional()` | ❌ | ❌ |
| `WHOP_REDIRECT_URI` | `process.env` | `z.string().url().optional()` | ❌ | ❌ |
| `SESSION_SECRET` | `process.env` | `z.string().optional()` | ❌ | ❌ (fallback: 'dev-secret') |
| `APP_HOST` | `process.env` | `z.string().url()` | ✅ | ❌ |

**Примечания:**
- `APP_HOST` — **обязательная** переменная (валидация `z.string().url()` без `.optional()`)
- Остальные — опциональные (`.optional()`)
- Все берутся из `process.env` через Zod валидацию
- Хардкод значений отсутствует (кроме fallback для `SESSION_SECRET`)

---

## 🔧 Команда для экспорта файла

### Windows (PowerShell):
```powershell
Copy-Item "app\src\config\env.ts" -Destination "env.ts.backup"
```

### Linux/Mac:
```bash
cp app/src/config/env.ts env.ts.backup
```

### Или через Git (если нужно экспортировать из репозитория):
```bash
git show HEAD:app/src/config/env.ts > env.ts.backup
```

### Универсальная команда (Node.js/npm):
```bash
# Создать директорию для экспорта
mkdir -p exports
# Скопировать файл
cp app/src/config/env.ts exports/env.ts
```

---

## 📝 Резюме

1. **Единственный файл инициализации:** `app/src/config/env.ts`
2. **Все переменные берутся из `process.env`** (нет хардкода)
3. **Валидация происходит через Zod схему** (строки 6-51)
4. **Переменные доступны через объект `env`** (строки 79-100)
5. **Источники:** `.env`, `.env.local`, системные переменные окружения
6. **Fallback:** только для `SESSION_SECRET` (`'dev-secret'` если не задан)

---

**Дата анализа:** 2025-11-02  
**Версия файла:** текущая (после интеграции Whop API)

