/**
 * Placeholder for Instagram DM integration.
 * Implement sendInstagram(instagramId: string, text: string) when API access is available.
 * Should return { messageId?: string } on success.
 */
export async function sendInstagram(_instagramId, _text) {
    return {};
}
