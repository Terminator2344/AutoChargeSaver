# Отчёт об устранении бесконечных редиректов (ERR_TOO_MANY_REDIRECTS)

**Обновлено:** Добавлены критические исправления для стабильной работы mock-режима

## 🔍 Первопричина проблемы

### Где возникал зацикленный редирект:
1. **Пользователь открывает `/dashboard`**
2. **`requireAuth` middleware** проверяет `req.session?.userId` → не найден
3. **Редирект на `/auth/whop`**
4. **`/auth/whop`** в mock-режиме редиректит на `/auth/dev`
5. **`/auth/dev`** создаёт пользователя, присваивает `req.session.userId`, но **сессия не сохранялась**
6. **Редирект на `/dashboard`** → возврат к шагу 2 (зацикливание)

### Конкретные проблемы:

#### 1. **Сессия не сохранялась перед редиректом** ⚠️ КРИТИЧНО
- **Файл:** `app/src/auth/whopOAuth.ts` (строка ~213-240)
- **Проблема:** `req.session.userId` присваивался, но `req.session.save()` не вызывался перед `res.redirect()`
- **Результат:** Браузер делал редирект, но cookie с сессией не отправлялись, поэтому следующий запрос не имел `userId`

#### 2. **Настройки сессии были слишком консервативными**
- **Файл:** `app/src/server.ts` (строка ~88-96)
- **Проблема:** `resave: false` и `saveUninitialized: false` не сохраняли сессию автоматически
- **Результат:** Требовался явный вызов `req.session.save()`

#### 3. **Отсутствовала защита от зацикливания**
- **Файл:** `app/src/middleware/auth.ts` (строка ~13-27)
- **Проблема:** `requireAuth` не проверял, находится ли запрос уже на auth-маршрутах
- **Результат:** Возможность редиректа на `/auth/whop` когда уже на `/auth/whop`

#### 4. **Конфликт между MOCK_LOGIN и WHOP_CLIENT_ID**
- **Файл:** `app/src/auth/whopOAuth.ts` (строка ~11-24)
- **Проблема:** Проверка `MOCK_LOGIN` могла быть неполной, если WHOP_CLIENT_ID был установлен
- **Результат:** Непредсказуемое поведение при наличии обоих настроек

#### 5. **Trust proxy в dev режиме**
- **Файл:** `app/src/server.ts` (строка ~120-131)
- **Проблема:** `trust proxy` мог влиять на cookie security в development
- **Результат:** Cookie могли не сохраняться из-за неправильного определения secure

---

## ✅ Применённые исправления

### 1. **Явное сохранение сессии в `/auth/dev`** ✅
**Файл:** `app/src/auth/whopOAuth.ts` (строки 225-241)

```typescript
req.session.userId = user.id;
req.session.whopUserId = user.whopUserId;

req.session.save((err) => {
  if (err) {
    return res.status(500).send('Failed to save session');
  }
  return res.redirect('/dashboard');
});
```

**Фикс:** Сессия теперь сохраняется **перед** редиректом, гарантируя что cookie установятся в браузере.

---

### 2. **Исправлены настройки сессии** ✅
**Файл:** `app/src/server.ts` (строки 88-97)

```typescript
session({
  secret: env.SESSION_SECRET || 'dev-secret',
  resave: true,                    // ✅ Было: false
  saveUninitialized: true,         // ✅ Было: false
  cookie: {
    secure: process.env.NODE_ENV === 'production', // ✅ false в dev
    httpOnly: true,
    sameSite: 'lax',
    maxAge: 7 * 24 * 60 * 60 * 1000
  }
})
```

**Фикс:** Сессия теперь сохраняется автоматически, даже если не изменена.

---

### 3. **Защита от зацикливания в requireAuth** ✅
**Файл:** `app/src/middleware/auth.ts` (строки 17-21)

```typescript
if (req.path === '/auth/whop' || req.path === '/auth/dev' || req.path === '/auth/whop/callback') {
  return next(); // Не редиректить, если уже на auth-маршруте
}
```

**Фикс:** Предотвращает редирект на `/auth/whop` если запрос уже на auth-маршруте.

---

### 4. **Приоритет MOCK_LOGIN** ✅
**Файл:** `app/src/auth/whopOAuth.ts` (строки 12-24)

```typescript
const mockLogin = process.env.MOCK_LOGIN === 'true' || env.MOCK_LOGIN;

if (mockLogin) {
  // ALWAYS redirect to /auth/dev if MOCK_LOGIN=true (highest priority)
  return res.redirect(`/auth/dev?email=dev@local.test&whop=mock_user_1`);
}
```

**Фикс:** `MOCK_LOGIN=true` имеет наивысший приоритет, даже если WHOP_CLIENT_ID установлен.

---

### 5. **Trust proxy в dev режиме** ✅
**Файл:** `app/src/server.ts` (строки 120-131)

```typescript
if (process.env.NODE_ENV === 'production') {
  app.set('trust proxy', 1);
} else {
  app.set('trust proxy', 0); // ✅ Явно отключено в dev
}
```

**Фикс:** В development режиме trust proxy отключён, cookie работают локально.

---

### 6. **Диагностические логи** ✅
**Добавлены логи во всех критических точках:**
- `[BOOT]` - при загрузке env
- `[AUTH/whop]` - проверка MOCK_LOGIN
- `[AUTH/dev] before/after` - состояние сессии
- `[AUTH/requireAuth]` - проверка авторизации
- `[DASHBOARD]` - рендеринг dashboard

**Фикс:** Легко отследить поток запросов и найти проблему.

---

### 7. **Debug endpoint** ✅
**Файл:** `app/src/server.ts` (строки 152-160)

```typescript
app.get('/debug/session', (req, res) => {
  res.json({
    sessionId: req.sessionID,
    keys: Object.keys(req.session || {}),
    userId: req.session?.userId,
    whopUserId: req.session?.whopUserId,
  });
});
```

**Фикс:** Позволяет проверить состояние сессии без необходимости логина.

---

## 🧪 Проверка после исправлений

### Ожидаемый поток (успешный):
```
1. GET /dashboard
   → [AUTH/requireAuth] NO userId → redirect /auth/whop

2. GET /auth/whop
   → [AUTH/whop] MOCK_LOGIN=true → redirect /auth/dev

3. GET /auth/dev?email=dev@local.test&whop=mock_user_1
   → [AUTH/dev] before userId=undefined
   → User upserted
   → [AUTH/dev] after set userId=<id>
   → req.session.save() → redirect /dashboard

4. GET /dashboard (с cookie)
   → [AUTH/requireAuth] OK userId=<id>
   → [DASHBOARD] Rendering dashboard
   → ✅ УСПЕХ
```

### Критерии приёмки:

✅ **npm run dev:mock → открыть http://localhost:3000/auth/whop**
- Автоматический редирект на `/auth/dev`
- Логи показывают: `[AUTH/whop] Mock mode → redirect /auth/dev`

✅ **GET /auth/dev создаёт пользователя и сохраняет сессию**
- Логи: `[AUTH/dev] session saved userId=<id>`
- Редирект на `/dashboard`

✅ **GET /dashboard открывается без редиректов**
- Логи: `[AUTH/requireAuth] OK userId=<id>`
- Dashboard рендерится успешно

✅ **GET /debug/session показывает { userId: <id> }**
- Сессия сохранилась корректно

✅ **Никаких циклов:**
- Чистый браузер (инкогнито) → работает
- Повторный заход (cookie есть) → работает
- MOCK_LOGIN=true + закомментированные WHOP_CLIENT_* → работает

---

## 📝 Итоговая причина одной строкой

**Основная причина:** Сессия не сохранялась перед редиректом из `/auth/dev`, из-за чего `req.session.userId` терялся между запросами, создавая бесконечный цикл `/dashboard` → `/auth/whop` → `/auth/dev` → `/dashboard` (без userId).

**Решение:** Добавлен явный вызов `req.session.save()` перед `res.redirect()` и изменены настройки сессии на `resave: true` / `saveUninitialized: true` для гарантированного сохранения.

---

## 📋 Список изменённых файлов

1. ✅ `app/src/server.ts` - настройки сессий, trust proxy, debug endpoint
2. ✅ `app/src/auth/whopOAuth.ts` - приоритет MOCK_LOGIN, явное сохранение сессии
3. ✅ `app/src/middleware/auth.ts` - защита от зацикливания, логирование
4. ✅ `app/src/ui/routes.ts` - диагностические логи
5. ✅ `README.md` - раздел Troubleshooting

---

## 🔧 Временные логи (можно оставить для отладки)

Все диагностические `console.log` можно оставить или обернуть в флаг:
```typescript
const DEBUG_SESSIONS = process.env.DEBUG_SESSIONS === 'true';
if (DEBUG_SESSIONS) {
  console.log('[AUTH/...]', ...);
}
```

В текущей версии логи активны для удобства диагностики в development режиме.

